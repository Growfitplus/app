import AntDesign from '@expo/vector-icons/AntDesign';

const ArrowNextIcon = (props: Record<string, unknown>) => (
  <AntDesign
    name='arrowright'
    {...props}
  />
);

export default ArrowNextIcon;
