import Ionicons from '@expo/vector-icons/Ionicons';

const ArrowBackIcon = (props: Record<string, unknown>) => (
  <Ionicons
    name='arrow-back'
    {...props}
  />
);

export default ArrowBackIcon;
