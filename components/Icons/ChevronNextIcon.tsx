import AntDesign from '@expo/vector-icons/AntDesign';

const ChevronNextIcon = (props: Record<string, unknown>) => (
  <AntDesign
    name='right'
    {...props}
  />
);

export default ChevronNextIcon;
